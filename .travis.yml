language: node_js

node_js:
  - '8.9.4'

before_install:
  - echo 'before_install'

install:
 - npm install mocha -g
 
cache:
  yarn: true
  directories:
    - node_modules

script:
  - yarn test #caso você tenha algum teste para ser rodado
  - bash ./teste.sh
  - bash ./deploy.sh